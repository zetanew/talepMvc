@{
    ViewData["Title"] = "Erişim Engellendi";
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Talebim.shop</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .denied-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            padding: 60px 40px;
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        .denied-icon {
            font-size: 80px;
            color: #dc3545;
            margin-bottom: 20px;
        }

        .denied-card h1 {
            color: #333;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .denied-card p {
            color: #666;
            margin-bottom: 30px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 10px;
            padding: 12px 30px;
            font-weight: 600;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>

<body>
    <div class="denied-card">
        <div class="denied-icon">
            <i class="bi bi-shield-x"></i>
        </div>
        <h1>Erişim Engellendi</h1>
        <p>Üzgünüz, bu sayfaya erişim yetkiniz bulunmuyor. Bunun bir hata olduğunu düşünüyorsanız lütfen sistem
            yöneticinizle iletişime geçin.</p>

        <div class="d-flex justify-content-center">
            <a asp-controller="Requests" asp-action="Dashboard" class="btn btn-primary">
                <i class="bi bi-house"></i> Ana Sayfaya Dön
            </a>
        </div>
    </div>
</body>

</html>